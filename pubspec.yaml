name: lottie
description: Lottie is a mobile library for Android and iOS that parses Adobe After Effects animations exported as json and renders them natively on mobile!. This package is a pure Dart implementation of a Lottie player.
version: 0.1.0
homepage: https://github.com/xvrh/lottie-flutter

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  logging:
  charcode:

dev_dependencies:
  flutter_test:
    sdk: flutter
  yaml:
  analyzer:
  dart_style:

#dependency_overrides:
#  pedantic: ^1.9.0
